#include <cuda.h>
#include <curand.h>

/*
curandGenerator_t gen;

curandCreateGenerator(&gen, CURAND_RNG_PSEUDO_DEFAULT);

curandSetPseudoRandomGeneratorSeed(gen, 1234ULL);

curandGeneratorNormal(gen, d_input, NUM_ELS, 0.0f, 1.0f);
*/

int main(void) {

